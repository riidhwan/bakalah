interface:
  display_name: "PR"
  short_description: "Prepare Bakalah commits and PRs"
  default_prompt: "Use $pr to prepare, commit, push, and open a Bakalah pull request."
policy:
  allow_implicit_invocation: true
