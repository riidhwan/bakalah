interface:
  display_name: "Compact Operations"
  short_description: "Keep command output compact"
  default_prompt: "Use $compact-operations while checking CI, releases, logs, searches, or other noisy command workflows."
policy:
  allow_implicit_invocation: true
